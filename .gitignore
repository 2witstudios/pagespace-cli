node_modules/
dist/
*.log
.DS_Store
.env
.env.*
.pi/
auth.json

# Local MCP config holds a scoped PageSpace token — never commit it
.mcp.json

# Harness-local dir (sessions/settings)
.claude/

# pure-point-guard per-project config (machine-absolute paths)
.pu/
